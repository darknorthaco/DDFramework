engine: v0.7.0 — DDFramework kernelization; stable API surface in ddf-core/

Tier 1. Full Constellation Loop. Single atomic commit. No layer
crates moved (Option A: conceptual kernel boundary, not physical).
Phase 5 delivers an embeddable kernel API with byte-for-byte
behavioral parity to the underlying engine.

Constellation Loop outcome:
  L1 system map        new stock: ddf-core/ (kernel boundary);
                       new flow: applications depend on ddf, ddf
                       delegates to phantom / ghost; boundary
                       enforced by tests, not by filesystem moves
  L2 domain            complicated (additive structure, declared API)
  L3 method            thin Rust crate + Python wrapper + simulation
                       stubs; zero new third-party deps
  L4 integrity         ddf crate re-exports phantom-core primitives
                       and shells out for CLI rituals; ddf_py
                       re-exports ghost; no duplicate logic
  L5 regenerative      heals the embedder's dependency story:
                       apps no longer need to know about the
                       phantom-core / hyperion-net / ghost-observer
                       internal layout; they consume `ddf` and stay
                       stable across internal refactors
  L6 learning loop     DDFRAMEWORK.md section 7.1 + ARCHITECTURE.md
                       section 0 document the boundary; kernelize
                       ritual (0007) added as the ceremonial record
  L7 simulation        PREDICTION: ddf verify output identical to
                                   phantom verify; clean state
                                   preserved; 55+ tests green;
                                   ledger grows by exactly doctrine
                                   .amended + 2 verify.result (one
                                   from phantom, one from ddf that
                                   shells to phantom) = 3 entries.
                       OUTCOME:    ddf verify dispatched to phantom
                                   and produced phantom's own
                                   output; ledger grew 11 -> 14
                                   (3 new entries, as predicted);
                                   advisory stream head unchanged;
                                   65 tests green (Rust 21 + Python
                                   ghost 33 + ddf-core 11).
                                   PREDICTION MATCHED.
  L8 disruption        low-end: app teams depending on internal
                       crates. Mitigated: DDFRAMEWORK.md 7.1
                       declares the internal crates may be
                       refactored freely; only ddf-core/ is stable.
                       new-market: embedding DDFramework in a
                       larger system now has a single integration
                       surface.
  L9 encode and enforce   ddf-core/ (Rust + Python + stubs + tests),
                       ceremonies/0007-kernelize.toml, doctrine
                       version bump to 0.7.0, workspace member added,
                       Makefile ddf target added. All test guards
                       in place.

Decisions bound (per operator, all ten):
  1. Option A (API facade; no physical moves).
  2. Irrelevant under A.
  3. Irrelevant under A.
  4. ddf CLI is a thin Rust bin that delegates to phantom.
  5. ddf_py is a NEW wrapper package under ddf-core/ddf_py/; it
     re-exports from ghost-observer without renaming.
  6. C ABI shrike_sock_* documented as part of the kernel contract;
     legacy names explicitly acknowledged; not renamed.
  7. Simulation stubs live under ddf-core/simulation/.
  8. 0007-kernelize.toml paths stay at repo root (no moves).
  9. Version bump 0.6.0 -> 0.7.0 (minor: new capability).
 10. Byte-identical ddf verify <=> phantom verify; enforced by
     tests/test_no_behavior_change.py.

New artifacts:
  ddf-core/README.md                        kernel API contract
  ddf-core/ddf/Cargo.toml                   Rust crate manifest
  ddf-core/ddf/src/lib.rs                   public API (API_VERSION,
                                            ENGINE_VERSION, verify,
                                            amend_doctrine, file_waiver,
                                            run_ritual, ledger::*,
                                            ghost::*, sha256, canonical,
                                            timestamp re-exports)
  ddf-core/ddf/src/main.rs                  `ddf` CLI binary
  ddf-core/ddf_py/pyproject.toml            Python package manifest
  ddf-core/ddf_py/README.md                 Python wrapper doc
  ddf-core/ddf_py/ddf/__init__.py           ddf.verify, ddf.advise,
                                            ddf.ledger, ENGINE_VERSION
  ddf-core/ddf_py/ddf/__main__.py           `python -m ddf` CLI
  ddf-core/simulation/README.md             Phase 6 scaffolding doc
  ddf-core/simulation/__init__.py           namespace package
  ddf-core/simulation/doctrine_diff.py      stub (NotImplementedError)
  ddf-core/simulation/ritual_dryrun.py      stub
  ddf-core/simulation/ledger_replay.py      stub
  ddf-core/simulation/advisory_replay.py    stub
  ddf-core/simulation/drift_simulation.py   stub
  ddf-core/tests/__init__.py                test package marker
  ddf-core/tests/test_kernel_structure.py   T1: required paths present,
                                            no application code inside
                                            kernel boundary
  ddf-core/tests/test_kernel_api.py         T2: Python ddf package
                                            imports cleanly; all
                                            re-exports resolve
  ddf-core/tests/test_no_behavior_change.py T3: latest verify.result
                                            entry's recorded hashes
                                            match current on-disk
                                            normalized hashes
  ceremonies/0007-kernelize.toml            kernelize ritual manifest

Modified:
  Cargo.toml                      workspace adds ddf-core/ddf;
                                  version 0.6.0 -> 0.7.0
  doctrine.toml                   0.6.0 -> 0.7.0; rituals.registered
                                  += [kernelize]
  phantom-core/src/lib.rs         DOCTRINE_VERSION 0.6.0 -> 0.7.0
  hyperion-net/src/lib.rs         test expected version 0.7.0
  ghost-observer/pyproject.toml   0.7.0
  ghost-observer/ghost/__init__.py     __version__ 0.7.0
  ghost-observer/ghost/advisor.py      GHOST_VERSION 0.7.0
  DDFRAMEWORK.md                  section 7.1 Kernelization added
  ARCHITECTURE.md                 section 0 kernel boundary diagram
  README.md                       status reflects v0.7.0; ddf targets
  Makefile                        PYTHONPATH += ddf-core/ddf_py;
                                  test gate discovers ddf-core;
                                  `make ddf ARGS="..."` target

Test results (target: 55+; achieved: 65):
  Rust      21/21 (3 new ddf lib tests + 18 pre-existing)
  Python ghost    33/33 (unchanged)
  Python ddf-core 11/11 (T1: 3 + T2: 6 + T3: 2)
  Total     65/65

Ledger state at commit:
  main ledger    14 entries, chain valid from genesis
    #12 doctrine.amended  v0.7.0   this commit's amendment
    #13 verify.result     v0.7.0   status=ok (phantom verify)
    #14 verify.result     v0.7.0   status=ok (ddf verify ->
                                   phantom verify; proves parity)
    head entry_hash sha256:c73806156cac5b5a0f6989761074448009c0003f232558ec63e8d0c92d90761e

  advisory stream 1 entry (bootstrap unchanged; no rules fired)
    head advisory_hash sha256:f6aaf82b75c5e2b8943852f716d6430b291e68e45baeefae855a2fb91b9e14ed

Doctrine version:
  architectural (doctrine.toml)    0.6.0 -> 0.7.0
  kernel API (ddf::API_VERSION)    0.1.0 (new)
  constitutional (constellation)   0.1.1   (unchanged)

Invariants: I1-I8 unchanged. Rituals registered: 7 (adds kernelize).
Rituals implemented: 4 executors (verify, amend-doctrine, file-waiver,
ghost-advise). kernelize is a one-time declarative ceremony recorded
here via amend-doctrine.

Explicit non-changes (legacy frozen per v0.6.0 policy):
  - phantom-core layer stays at repo root.
  - hyperion-net layer stays at repo root.
  - ghost-observer layer stays at repo root.
  - ledger/, advisories/, constellation.* , doctrine.* stay at root.
  - Shrike-prefixed C ABI (shrike_sock_*) unchanged; documented as
    kernel contract despite legacy prefix.
  - phantom CLI banner text "Shrike Phantom Core" unchanged.
  - LICENSE, pyproject metadata legacy fields unchanged.
  - Invariant labels "Shrike I1-I8" unchanged in existing code.

Behavioral parity evidence:
  `ddf verify` emitted identical output to `phantom verify` (printed
  header "phantom verify", same status, same hashes). The ddf binary
  is a transparent dispatcher.

Honest notes:
  - The test test_no_behavior_change.py INTENTIONALLY checks the
    latest verify.result entry against current on-disk hashes.
    It failed before the ceremony (ledger last entry was v0.6.0) and
    passed after (ledger gained v0.7.0 verify.result entries). That
    is the designed behavior of the test and of the ceremony order.
  - Windows Application Control did NOT block any binary in this
    run; 21/21 Rust tests executed.

Reversibility: git revert returns to v0.6.0 state. The ddf-core/
directory becomes untracked, the workspace loses the ddf crate,
the ledger retains entries #12-#14 as append-only historical record.
